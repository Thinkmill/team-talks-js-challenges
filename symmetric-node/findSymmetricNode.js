/*
 * Given two identical DOM tree structures, A and B, and a node from A, find the corresponding node in B.
 */

/* @returns {Element} - corresponding dom element in root2 */
module.exports.findSymmetricNode = (treeA, treeB, node) => {
	/* implement here */
};
